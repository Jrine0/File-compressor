module.exports = {
  plugins: [
    '@tailwindcss/postcss',  // Use the new PostCSS plugin for Tailwind CSS
    'autoprefixer',           // Optional: Autoprefixer for better browser support
  ],
}